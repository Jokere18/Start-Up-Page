import random

num_choice = input("Choose two numbers separated by a space (e.g., '1 20') for me to guess a number in between: ")
num1, num2 = map(int, num_choice.split())

guessed_number = random.randint(num1 + 1, num2 - 1)
print("I guess:", guessed_number)
